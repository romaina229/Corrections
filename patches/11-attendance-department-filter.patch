diff --git a/src/pages/attendance/Attendance.tsx b/src/pages/attendance/Attendance.tsx
--- a/src/pages/attendance/Attendance.tsx
+++ b/src/pages/attendance/Attendance.tsx
@@
 import { useQuery, useQueryClient } from '@tanstack/react-query';
@@
     const [date, setDate] = useState<string>(new Date().toISOString().split('T')[0]);
+    const [departmentId, setDepartmentId] = useState<string>('');
+
+    const { data: departmentsData } = useQuery({
+        queryKey: ['departments', 'attendance-filter'],
+        queryFn: async () => (await axios.get('/departments')).data,
+        staleTime: 5 * 60_000,
+    });
@@
-        queryKey: ['attendance', 'today', date],
-        queryFn: async () => (await axios.get('/attendances/today', { params: { date } })).data,
+        queryKey: ['attendance', 'today', date, departmentId],
+        queryFn: async () => (await axios.get('/attendances/today', {
+            params: { date, ...(departmentId ? { department_id: departmentId } : {}) },
+        })).data,
@@
                 <div className="flex items-center space-x-3">
+                    <select
+                        value={departmentId}
+                        onChange={(e) => setDepartmentId(e.target.value)}
+                        className="px-3 py-2 border border-gray-300 rounded-md sm:text-sm"
+                    >
+                        <option value="">Tous les départements</option>
+                        {(departmentsData?.data ?? departmentsData ?? []).map((department: any) => (
+                            <option key={department.id} value={department.id}>
+                                {department.name}
+                            </option>
+                        ))}
+                    </select>
                     <input
*** End Patch
