diff --git a/app/Http/Controllers/Api/AttendanceController.php b/app/Http/Controllers/Api/AttendanceController.php
index a26eeed..2213aa3 100644
--- a/app/Http/Controllers/Api/AttendanceController.php
+++ b/app/Http/Controllers/Api/AttendanceController.php
@@ -60,7 +60,7 @@ public function clockIn(Request $request)
 
         $this->assertEmployeeAccess((int) $data['employee_id']);
 
-        return response()->json($this->recordClockIn(
+        $result = $this->recordClockIn(
             (int) $data['employee_id'],
             $data['method'] ?? 'manual',
             $data['notes'] ?? null,
@@ -68,7 +68,13 @@ public function clockIn(Request $request)
                 'latitude' => $data['latitude'] ?? null,
                 'longitude' => $data['longitude'] ?? null,
             ], static fn ($value) => $value !== null)
-        ));
+        );
+
+        if (! empty($result['error'])) {
+            return response()->json($result, 422);
+        }
+
+        return response()->json($result);
     }
 
     private function recordClockIn(int $employeeId, string $method, ?string $notes, array $locationData): array
@@ -83,6 +89,7 @@ private function recordClockIn(int $employeeId, string $method, ?string $notes,
             return [
                 'message' => 'Vous avez déjà pointé aujourd\'hui',
                 'attendance' => $existing,
+                'error' => true,
             ];
         }
 
@@ -124,7 +131,7 @@ public function clockOut(Request $request)
 
         if (! $attendance) {
             return response()->json([
-                'message' => 'Aucun pointage entrée trouvé pour aujourd\'hui',
+                'message' => 'Aucun pointage d\'entrée trouvé pour aujourd\'hui',
             ], 404);
         }
 
@@ -258,11 +265,11 @@ public function scanQR(string $qrCode)
 
         $result = $this->recordClockIn((int) $employeeId, 'qr_code', null, []);
 
-        if (isset($result['attendance'])) {
-            return response()->json($result);
+        if (! empty($result['error'])) {
+            return response()->json($result, 422);
         }
 
-        return response()->json($result, 422);
+        return response()->json($result);
     }
 
     public function stats(Request $request)
