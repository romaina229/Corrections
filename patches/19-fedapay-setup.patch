diff --git a/.env.example b/.env.example
index 0b90523..02828fb 100644
--- a/.env.example
+++ b/.env.example
@@ -68,4 +68,11 @@ FRONTEND_URL=http://localhost:3000
 FRONTEND_URLS=http://localhost:3000
 SANCTUM_STATEFUL_DOMAINS=localhost:3000
 FILESYSTEM_DISK=public
+
+# FedaPay — clés Sandbox tant que le compte n'est pas passé en Live.
+# Clé secrète et clé webhook : jamais exposées côté frontend.
+FEDAPAY_SECRET_KEY=sk_sandbox_xxx
+FEDAPAY_PUBLIC_KEY=pk_sandbox_xxx
+FEDAPAY_ENVIRONMENT=sandbox
+FEDAPAY_WEBHOOK_SECRET=wh_sandbox_xxx
 DEMO_ADMIN_PASSWORD=
diff --git a/app/Models/Payment.php b/app/Models/Payment.php
new file mode 100644
index 0000000..518a3e1
--- /dev/null
+++ b/app/Models/Payment.php
@@ -0,0 +1,41 @@
+<?php
+
+namespace App\Models;
+
+use App\Traits\BelongsToTenant;
+use Illuminate\Database\Eloquent\Model;
+
+class Payment extends Model
+{
+    use BelongsToTenant;
+
+    protected $fillable = [
+        'subscription_id',
+        'provider',
+        'provider_transaction_id',
+        'plan',
+        'billing_cycle',
+        'amount',
+        'currency',
+        'status',
+        'checkout_url',
+        'raw_payload',
+        'paid_at',
+    ];
+
+    protected $casts = [
+        'amount' => 'decimal:2',
+        'raw_payload' => 'array',
+        'paid_at' => 'datetime',
+    ];
+
+    public function tenant()
+    {
+        return $this->belongsTo(Tenant::class);
+    }
+
+    public function subscription()
+    {
+        return $this->belongsTo(Subscription::class);
+    }
+}
diff --git a/composer.json b/composer.json
index b1e10a8..ddd31f5 100644
--- a/composer.json
+++ b/composer.json
@@ -11,6 +11,7 @@
     "require": {
         "php": "^8.3",
         "barryvdh/laravel-dompdf": "^3.1",
+        "fedapay/fedapay-php": "^1.5",
         "laravel/framework": "^13.8",
         "laravel/sanctum": "^4.3",
         "laravel/tinker": "^3.0",
diff --git a/config/services.php b/config/services.php
index 6a90eb8..a82dde3 100644
--- a/config/services.php
+++ b/config/services.php
@@ -35,4 +35,11 @@
         ],
     ],
 
+    'fedapay' => [
+        'secret_key' => env('FEDAPAY_SECRET_KEY'),
+        'public_key' => env('FEDAPAY_PUBLIC_KEY'),
+        'environment' => env('FEDAPAY_ENVIRONMENT', 'sandbox'),
+        'webhook_secret' => env('FEDAPAY_WEBHOOK_SECRET'),
+    ],
+
 ];
diff --git a/database/migrations/2026_08_16_090001_create_payments_table.php b/database/migrations/2026_08_16_090001_create_payments_table.php
new file mode 100644
index 0000000..207d562
--- /dev/null
+++ b/database/migrations/2026_08_16_090001_create_payments_table.php
@@ -0,0 +1,34 @@
+<?php
+
+use Illuminate\Database\Migrations\Migration;
+use Illuminate\Database\Schema\Blueprint;
+use Illuminate\Support\Facades\Schema;
+
+return new class extends Migration {
+    public function up(): void
+    {
+        Schema::create('payments', function (Blueprint $table) {
+            $table->id();
+            $table->foreignId('tenant_id')->constrained()->cascadeOnDelete();
+            $table->foreignId('subscription_id')->nullable()->constrained()->nullOnDelete();
+            $table->string('provider')->default('fedapay');
+            $table->string('provider_transaction_id')->nullable()->index();
+            $table->string('plan');
+            $table->string('billing_cycle')->default('monthly');
+            $table->decimal('amount', 15, 2);
+            $table->string('currency', 3)->default('XOF');
+            $table->enum('status', ['pending', 'approved', 'declined', 'canceled'])->default('pending');
+            $table->text('checkout_url')->nullable();
+            $table->json('raw_payload')->nullable();
+            $table->timestamp('paid_at')->nullable();
+            $table->timestamps();
+
+            $table->unique(['provider', 'provider_transaction_id']);
+        });
+    }
+
+    public function down(): void
+    {
+        Schema::dropIfExists('payments');
+    }
+};
