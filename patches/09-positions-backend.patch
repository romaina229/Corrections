diff --git a/app/Http/Controllers/Api/PositionController.php b/app/Http/Controllers/Api/PositionController.php
index 8dc2480..000aa13 100644
--- a/app/Http/Controllers/Api/PositionController.php
+++ b/app/Http/Controllers/Api/PositionController.php
@@ -65,6 +65,11 @@ public function store(Request $request)
 
     public function show(Position $position)
     {
+        abort_unless(
+            (int) $position->tenant_id === (int) app('tenant')->id,
+            404
+        );
+
         return response()->json([
             'position' => $position->load(['department', 'employees.user'])
         ]);
@@ -72,6 +77,11 @@ public function show(Position $position)
 
     public function update(Request $request, Position $position)
     {
+        abort_unless(
+            (int) $position->tenant_id === (int) app('tenant')->id,
+            404
+        );
+
         $request->validate([
             'title' => 'sometimes|string|max:255',
             'corps' => 'nullable|string|max:255',
@@ -105,6 +115,11 @@ public function update(Request $request, Position $position)
 
     public function destroy(Position $position)
     {
+        abort_unless(
+            (int) $position->tenant_id === (int) app('tenant')->id,
+            404
+        );
+
         if ($position->employees()->count() > 0) {
             return response()->json([
                 'message' => 'Impossible de supprimer ce poste car il a des employés'
